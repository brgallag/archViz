(function() {
    'use strict';
    angular.module('module-app')
    .service('websocketService', ['$q', '$rootScope',
    function ($q, $rootScope) {

        var service = {
            
        };

        return service;
        
    }]);
})();